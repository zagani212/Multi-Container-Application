services:
  api:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
  mongo:
    image: "mongo:latest"
    volumes: 
      - mongodb_volume:/data/db 
volumes:
  mongodb_volume: